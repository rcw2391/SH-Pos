<UserControl x:Class="Pos.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:local="clr-namespace:Pos.Views"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <telerik:RadGridView ShowGroupPanel="False" EnableRowVirtualization="True" AutoGenerateColumns="True" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" MaxHeight="600" MaxWidth="800" IsReadOnly="True"/>

        <TextBox Grid.Row="1" Text="{Binding SelectedCustomer.FirstName}"/>
        <Button Grid.Row="2" Command="{Binding ClickMeCommand}">Save</Button>
        <Button Grid.Row="3" Command="{Binding DeleteCommand}">Delete</Button>
        <Button Grid.Row="4" Command="{Binding InsertCommand}">Insert</Button>
    </Grid>
</UserControl>
